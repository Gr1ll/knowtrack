# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type EntryEntity {
  id: ID!
  topicId: String!
  content: String!
  description: String
  progress: Float!
  createdAt: DateTime!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type TopicEntity {
  id: ID!
  userId: String!
  title: String!
  description: String
  entries: [EntryEntity!]!
  createdAt: DateTime!
}

type UserEntity {
  id: ID!
  email: String
  wallet: String
  topics: [TopicEntity!]
  createdAt: DateTime!
}

type Query {
  users: [UserEntity!]!
  user(id: String, email: String): UserEntity
  topics: [TopicEntity!]!
  topic(id: String): TopicEntity
  entries: [EntryEntity!]!
  entry(id: String): EntryEntity
}

type Mutation {
  createUser(email: String!): UserEntity!
  createTopic(title: String!, description: String!, userId: String!): TopicEntity!
  createEntry(topic: String!, content: String!): EntryEntity!
}